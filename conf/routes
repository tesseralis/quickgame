# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index
# Display landing page for a game
GET     /:gameName                  controllers.Application.gameIndex(gameName)
# Create a new game
GET     /:gameName/new              controllers.Application.newGame(gameName)
# Display a single game
GET     /:gameName/:id              controllers.Application.game(gameName, id)
# Websocket stream
GET     /:gameName/:id/stream       controllers.Application.stream(gameName, id)

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
